# 宇树 G1

本文档说明 G1 在本工作区中有哪些控制入口、各自负责什么，以及它们对应的官方 ROS 2 话题。先按需求选择控制路径，再查后面的消息和 API 细节。

| 需求 | 使用入口 | 责任边界 |
|---|---|---|
| 行走、站立和内置动作 | `/api/sport/*`、`/api/arm/*` | Unitree 固件负责轨迹、平衡和电机控制 |
| Unitree 行走 + 自定义上肢 | `/api/sport/*` + `/arm_sdk` | 固件控制下肢，用户发布腰部和手臂目标 |
| 本工作区整机位置控制 | FPC 或 JTC | controller 生成 31 轴位置，`G1TopicSystem` 补齐 MIT 字段并发布 `/lowcmd` 和夹爪命令 |
| 完全自定义底层控制 | 直接 `/lowcmd` | 用户负责全部本体关节、安全、模式、MIT 参数和 CRC；不能保留内置行走 |

## 本工作区控制结构

机器人本体使用官方 [`unitree_ros2`](https://github.com/unitreerobotics/unitree_ros2) 消息定义。G1 只需要以下两个包：

- `unitree_api`：机器人服务请求/响应消息。
- `unitree_hg`：G1/H1 系列的状态与控制消息。

本项目不编译 `unitree_go` 和 `unitree_ros2_example`。

整机控制使用 ROS 2 Humble 原生 `ros2_control`：

- `unitree_g1_description` 只保存整机模型、mesh 和硬件无关的 ros2_control 资源声明；
- `unitree_g1_ros2_control` 提供真实 `SystemInterface`、C++ forward-position controller、标准 JointTrajectoryController 实例和状态 broadcaster；
- `robot_bringup` 只组合设备与控制栈，`whole_body_dashboard.launch.py` 只连接已有 `/controller_manager`；
- 同一份展开 URDF 同时交给 `controller_manager` 与 `robot_state_publisher`，不存在第二套 controller-manager facade。

统一硬件插件导出 31 个位置命令接口（G1 29 轴和左右 Gloria-M 偏心轴），另有 93 个关节状态接口、62 个关节 `kp`/`kd` 状态接口、双 KWR57 六轴力状态以及两个 IMU：`pelvis_imu`（`/lowstate.imu_state`）和 `torso_imu`（`/secondary_imu`）。`forward_position_controller`（FPC）与 `joint_trajectory_controller`（JTC）请求完全相同的 31 个接口，由 controller_manager 保证只能激活其中一个。claim 只表示命令所有权，不自带反馈时效语义；插件分别以 `0.25 s` 检查 G1、以 `0.75 s` 检查 Gloria。夹爪反馈过期时只跳过对应 MIT 输出，反馈恢复后自然恢复，不中断本体 LowCmd。manager 以 500 Hz 调用硬件 `read()/write()`，KWR57 原始 1 kHz 话题保持不变。


## G1 官方话题、控制方法与模式
本节只整理 `src/unitree_ros2` 当前源码中可验证的 G1 接口，并补充与该版本相关的官方固件接口。

表中的名称是 `ros2 topic list` 所见的 ROS 2 名称。官方 DDS 文档常写 `rt/lowcmd`、`rt/arm_sdk`；`rt/` 是 ROS 2 到 DDS 的底层话题前缀，在本仓库的 ROS 2 节点中应使用 `/lowcmd`、`/arm_sdk`，不要改成 `/rt/lowcmd`。

### G1 话题速查

#### 本体、上肢与高层服务

| ROS 2 话题 | 消息类型 | 方向 | 用途与当前源码依据 |
|---|---|---|---|
| `/lowstate` | `unitree_hg/msg/LowState` | G1 -> PC2 | 高频整机状态：`mode_pr`、`mode_machine`、**盆骨 IMU**、35 个消息槽位的 `motor_state`、遥控器字节和 CRC。G1 示例使用前 29 个本体电机状态，并注明高频流为 500 Hz。 |
| `/lf/lowstate` | `unitree_hg/msg/LowState` | G1 -> PC2 | `/lowstate` 的低频版本；具体频率由固件决定。`g1_low_level_example` 通过编译期 `HIGH_FREQ` 在二者间选择。 |
| `/lowcmd` | `unitree_hg/msg/LowCmd` | PC2 -> G1 | 全身低层电机命令，包含 `q/dq/tau/kp/kd`、`mode_pr`、`mode_machine` 和 CRC。它接管 G1 本体控制，不能与内置行走控制或另一个 `/lowcmd` 发布者并行使用。❗ **运控模式占用时固件自己就在这个话题上发 1 kHz 全零帧**，详见「待机的 `ai` 模式在以 1 kHz 发全零 `/lowcmd`」。 |
| `/secondary_imu` | `unitree_hg/msg/IMUState` | G1 -> PC2 | **躯干 IMU**，安装位置对应 URDF 的 `imu_in_torso`；`g1_ankle_swing_example` 单独订阅该话题。与 `/lowstate.imu_state`（盆骨）是两颗不同的物理传感器，详见下一节。 |
| `/arm_sdk` | `unitree_hg/msg/LowCmd` | PC2 -> G1 | 高层运动服务下的腰部/上肢混合接管。**官方明确：只有 12–28（腰部与上肢）和 29（权重，取值 [0.0, 1.0]）有效，其余元素均无效**；腿部仍由内置移动控制器控制。只能在特定 FSM 下使用，见「Arm SDK 的可用模式」。 |
| `/api/sport/request` | `unitree_api/msg/Request` | PC2 -> G1 | `LocoClient` 高层运动请求：FSM、平衡模式、速度、摆腿/站立高度和内置上肢任务。 |
| `/api/sport/response` | `unitree_api/msg/Response` | G1 -> PC2 | 与 request 的 `identity.id` 配对，返回状态码和 JSON 数据。当前 `BaseClient` 的单次等待上限为 5 秒。 |
| `/api/arm/request` | `unitree_api/msg/Request` | PC2 -> G1 | `G1ArmActionClient` 的预定义上肢动作和动作列表请求。 |
| `/api/arm/response` | `unitree_api/msg/Response` | G1 -> PC2 | Arm Action 响应；API ID 与 sport 服务分别解释，即使数值相同也不是同一个调用。 |
| `/api/motion_switcher/request` | `unitree_api/msg/Request` | PC2 -> G1 | 查询、选择或释放当前运动服务所有权，也可设置 silent 状态。 |
| `/api/motion_switcher/response` | `unitree_api/msg/Response` | G1 -> PC2 | MotionSwitcher 响应。全身低层接管前应先确认内置运动服务已经释放。 |
| `/api/voice/request` | `unitree_api/msg/Request` | PC2 -> G1 | TTS、PCM 播放、音量和头部 RGB 灯控制；属于外设控制，不改变运动控制权。 |
| `/api/voice/response` | `unitree_api/msg/Response` | G1 -> PC2 | Voice API 响应。 |

`LowCmd` 和 `LowState` 的 ROS 消息固定有 35 个电机槽位，但 G1 23/29-DoF 本体只使用与硬件配置对应的前 29 个索引。槽位 29–34 是 Unitree 消息预留位，不是本项目两只 Gloria-M 的电机；Gloria-M 使用独立的 `gloria_ros` 消息和 CAN 驱动。只有在 `/arm_sdk` 协议中，索引 29 的 `q` 字段被重新定义为上肢接管权重。

#### 官方 29-DoF 电机索引

来自官方《关节电机顺序》：

```
0–5    左腿   hip_pitch / hip_roll / hip_yaw / knee / ankle_pitch / ankle_roll
6–11   右腿   同上顺序
12–14  腰     waist_yaw / waist_roll / waist_pitch
15–21  左臂   shoulder_pitch / _roll / _yaw / elbow / wrist_roll / _pitch / _yaw
22–28  右臂   同上顺序
```

`mode_pr = 1`（AB 并联模式）下，**只有并联机构那几个索引换一种解释**，不是另外的电机：
索引 4/5、10/11 的 `ankle_pitch/roll` → `ankle_B/A`，索引 13/14 的 `waist_roll/pitch` → `waist_A/B`。
其余索引不变。

这也是本工作区 `G1TopicSystem::kExpectedJointNames` 前 29 项的顺序（追加的 29/30 是两只
Gloria-M 夹爪的偏心轴，走 CAN，**与 `/arm_sdk` 权重槽 29 只是编号巧合**）。
23-DoF 机型在 13/14、20/21、27/28 上是空槽。

#### 两个 IMU 的安装位置

G1 身上有**两颗独立 IMU**，分布在腰关节的两侧。选错会在弯腰时直接把重力方向算错：

| 话题 / 字段 | 物理位置 | URDF link | 本工作区导出 |
|---|---|---|---|
| `/lowstate.imu_state`（主 IMU） | 盆骨 | `imu_in_pelvis` | `hardware_interface` 的 `pelvis_imu` sensor，广播到 `/pelvis_imu_broadcaster/imu` |
| `/secondary_imu`（第二 IMU） | 躯干 | `imu_in_torso` | 当前未接入 `hardware_interface`，需要的节点直接订阅该话题 |

两者之间隔着 `waist_yaw_joint`、`waist_roll_joint`、`waist_pitch_joint` 三个关节（URDF 链：`pelvis → waist_yaw(z) → waist_roll(x) → waist_pitch(y) → torso_link`），因此
$$R_{pelvis \leftarrow torso} = R_z(q_{yaw})\,R_x(q_{roll})\,R_y(q_{pitch})$$

**2026-07-27 实机验证**：把 `/secondary_imu` 的重力方向经该旋转变换后能重现 `/lowstate.imu_state`，
在两种差异很大的腰姿下残差都稳定在 **1.15–1.19°**（传感器固定安装/零偏差异），而反向假设的残差
从 8.91° 恶化到 19.68°。上半身保持竖直、只弯腰时，两者倾角差 `15.94−5.93=10.01°` 与腰 pitch
`9.48°` 吻合——倾角大的那颗确实在盆骨。

使用原则：
- 模型根在 `torso_link` 的计算（如双臂重力补偿）直接用 `/secondary_imu`，`imu_in_torso_joint` 的 `rpy` 为零，不需要额外旋转。
- 模型根在 `pelvis` 的计算用 `/lowstate.imu_state`。
- 跨腰使用时必须带上三个腰关节角做旋转，不能直接当成同一个坐标系。


#### 固件有但当前源码快照未封装的接口

| 官方 DDS 名称 | 对应 ROS 2 名称 | 状态 |
|---|---|---|
| `rt/sportmodestate` | `/sportmodestate` | G1 固件 1.5.1 以后发布，官方 IDL 只有四个字段：`unsigned long fsm_id`、`unsigned long fsm_mode`、`unsigned long task_id`、`float task_time`。✅ 实测该话题在线（类型 `unitree_hg/msg/SportModeState`，1 发布者），**但本快照的 `unitree_hg/msg` 目录没有 `SportModeState.msg`，所以本地无法订阅**；需更新匹配固件的 `unitree_hg` 或自行加入官方 IDL。不要用基于 `unitree_go/SportModeState` 的通用示例代替 G1 类型。 |
| `rt/user_lowcmd` | `/user_lowcmd` | 官方「用户开发模式」的全身临时接管话题，详见下方。✅ `unitree_sdk2` 已实现（`SwitchToUserCtrl` 7110 / `SwitchToInternalCtrl` 7111，示例 `g1_userctrl_dds_example.cpp`），但**本工作区的 `src/unitree_ros2` 快照不含这两个调用**——需要时直接按 api_id 发 `Request`。实测 `/loco_sdk`（`unitree_hg/msg/LowState`）0 发布者 1 订阅者，用途未知。 |

`SportModeState` 字段含义（官方）：

| 字段 | 含义 |
|---|---|
| `fsm_id` | 模式 id，见「专家接口」模式表 |
| `fsm_mode` | `0` 静态，允许向外切换至其他模式；`1` 动态，不允许切换到大部分模式 |
| `task_id` | 上肢互动动作 id |
| `task_time` | 上肢动作执行时间（秒），从 0 递增直到动作完成；**动作为握手时，保持期间此值不变** |

用户开发模式（User Development Mode）——官方定位是「需要迅速在自定义全身控制器和宇树内置运控之间转换」时使用，比
`MotionSwitcher` 的释放/恢复更轻量。**此模式下所有电机的控制权全部交给用户。**

| 接口 | api_id | 作用 |
|---|---:|---|
| `SwitchToUserCtrl()` | 7110 | 从当前模式切换至用户开发模式，之后用 `rt/user_lowcmd` 发底层指令 |
| `SwitchToInternalCtrl(mode)` | 7111 | 切回内置运控；`LAST`(0) = 切入前的运控模式，`PASSIVE`(1) = 阻尼，`WALKRUN`(2) = 走跑模式 |

✅ `g1_userctrl_dds_example.cpp` 里有两条官网正文没写、但实现上必须遵守的前提：

1. **切入前 `fsm_id` 必须是 1（阻尼）。** example 开头就查，不是 1 直接
   `Current fsm is not PASSIVE, exitting` 退出。
2. **调 `SwitchToUserCtrl()` 之前必须先往 `rt/user_lowcmd` 发一帧 `q=0, kp=0, kd=kd` 的空命令**，
   先把通道建起来。否则切过去的那一瞬间没有有效命令。切之后才从实测位置用 2 s 插值到目标。

`rt/user_lowcmd` 的帧和 `/arm_sdk` 一样，**只填 `q/dq/tau/kp/kd`，不设 crc / mode_machine /
mode_pr / motor_cmd.mode**。与 `/arm_sdk` 的区别是它接管全部 29 个电机（含腿），且**没有权重槽**。

`unitree_hg` 还定义了 `BmsState`、`MainBoardState` 等消息类型，但当前 G1 示例和头文件没有把它们绑定到确定的话题名，因此不能仅凭消息名推断机器人一定发布同名 topic。现场应以 `ros2 topic list -t` 为准。

`/joint_states`、`/robot_description`、`/tf`、`/pelvis_imu_broadcaster/imu`、FPC/JTC 命令接口以及 KWR57/Gloria-M 话题，是本工作区在官方 G1 接口之上生成的 ROS 2 接口，不是 G1 固件原生 topic；它们在后续“启动入口”和各功能包 README 中说明。

### 控制方法与控制权

| 控制路径 | 入口 | 控制范围 | 保留内置行走 | 主要约束 |
|---|---|---|---|---|
| Loco 高层运动 | `/api/sport/*` | 整机模式、平面速度、步态参数、内置上肢任务 | 是 | 输入的是机身速度/模式目标，不是腿关节命令；最终模式切换由固件状态机决定。 |
| Arm Action | `/api/arm/*` | 固件内置的挥手、握手等动作 | 是 | 仅支持指定 FSM；与 `/arm_sdk` 互斥。动作 ID 应通过 `GetActionList()` 查询，不应按其他固件版本硬编码。 |
| Arm SDK 混合接管 | `/arm_sdk` | 腰部和双臂索引 12–28 | ❗ **官方声称保留，实测不保留** | 索引 29 为权重；持续发布期间 Arm Action 会返回“arm_sdk occupied”。**仅能在锁定站立与运控模式下使用**；且**权重 > 0 时下半身不再接受 API 速度指令**，见「Arm SDK 接管与内置行走互斥」。 |
| MotionSwitcher | `/api/motion_switcher/*` | 运动服务所有权，不直接产生关节目标 | 取决于所选服务 | `CheckMode()`/`SelectMode()`/`ReleaseMode()` 管理当前运动服务；释放内置服务后，高层 Loco 将失效，直到重新选择运动服务。 |
| 全身低层控制 | `/lowcmd` + `/lowstate` | G1 所有可用本体电机 | 否 | 用户负责姿态、平衡和安全；必须先释放内置运动服务，正确设置模式与 CRC，并保证只有一个命令发布者。 |
| User Development Mode | 新版客户端 + `/user_lowcmd` | 全部电机（官方：“所有电机的控制权全部交给用户”） | 否 | 官方定位为“迅速在自定义全身控制器和内置运控之间转换”，比 MotionSwitcher 释放/恢复更轻量；需 `SwitchToUserCtrl()` / `SwitchToInternalCtrl()`。**本源码快照未实现这两个调用。** |
| Dex3 | `/dex3/*` | 独立灵巧手 | 是 | 仅适用于安装了 Dex3 的机器人；不占 G1 前 29 个本体电机索引。 |
| Voice | `/api/voice/*` | 扬声器、音量和 RGB 灯 | 是 | 非运动控制。 |

需要“自己控制上半身、Unitree 控制下半身”时，正确组合是 `/api/sport/*` 加 `/arm_sdk`。不要使用 `/lowcmd`，也不要运行会调用 `MotionSwitchClient::ReleaseMode()` 的 `g1_dual_arm_example`；该示例虽然名为 dual arm，实际会先停掉内置运动服务，再进入全身低层控制。

### Loco 高层 API

`LocoClient` 使用 `/api/sport/request` 和 `/api/sport/response`。API ID 只在该 endpoint 内有意义。

| API ID | 方法 | 参数/结果 | 含义 |
|---:|---|---|---|
| 7001 | `GetFsmId(int&)` | 输出 `fsm_id` | 获取运控所在模式。 |
| 7002 | `GetFsmMode(int&)` | 输出 `fsm_mode` | 动态/静态标志，官方有两处措辞，见「FSM ID」节。 |
| 7003 | `GetBalanceMode(int&)` | 输出 `balance_mode` | 平衡站立或持续踏步。 |
| 7004 | `GetSwingHeight(float&)` | 输出摆腿高度 | 固件会约束有效范围。 |
| 7005 | `GetStandHeight(float&)` | 输出站立高度 | |
| 7006 | `GetPhase(vector<float>&)` | 输出步态相位 | 已标记 deprecated。 |
| 7101 | `SetFsmId(int)` | `fsm_id` | 设置运控模式。**调用成功只表示请求被接受，最终状态仍需再次查询**；非法目标会被静默丢弃。 |
| 7102 | `SetBalanceMode(int)` | `balance_mode` | 设置平衡/持续踏步策略。 |
| 7103 | `SetSwingHeight(float)` | 摆腿高度 | 设置步态参数。 |
| 7104 | `SetStandHeight(float)` | 站立高度 | 设置步态参数。 |
| 7105 | `SetVelocity(vx, vy, omega, duration)` | 前向、横向、偏航速度和有效秒数 | 机身平面速度设定值。官方备注：「程序会自动设置裁剪到允许范围」。 |
| 7106 | `SetTaskId(int)` | 内置上肢任务 ID | 与 `/api/arm` 的 7106 是不同 endpoint 下的不同 API。 |
| 7107 | `SetSpeedMode(int)` | 0–3 | 官方：「**走跑模式下**调整最高速度」。 |
| **7110** | `SwitchToUserCtrl()` | `{"data": false}`（内容无关） | 切入用户开发模式。见「用户开发模式」。 |
| **7111** | `SwitchToInternalCtrl(mode)` | `{"data": 0\|1\|2}` | 切回内置运控；`0`=LAST、`1`=PASSIVE、`2`=WALKRUN。 |

❗ **7110/7111 在官网的 API 表里没有列出编号**，以上取自 `unitree_sdk2` 的 `g1_loco_api.hpp`（服务名
`LOCO_SERVICE_NAME = "sport"`）。本工作区的 `src/unitree_ros2` 快照不包含这两个调用，需要时直接按 api_id 发 `Request`。

❗ **7003–7006 和 7102–7104（平衡模式 / 摆腿高度 / 站高）在本机全部不可用**，
读返回 7301、写无效。它们属于常规运控，见「站高 / 平衡模式 / 摆动高度属于『常规运控』」。

##### 实测扫描：这台固件在 sport endpoint 上到底实现了哪些 api_id

对 7001–7112 逐个发空参数请求，按响应码分类（2026-07-29，`ai` 服务）：

| api_id | 响应 | 结论 |
|---:|---|---|
| 7007 | `code=0 {"data":true}` | **`GetArmSdkStatus`**——只出现在 H2 的头文件里，G1 的没有，但**固件实现了**，且当前为启用 |
| 7108 | `3202` | **存在但两份头文件都查不到**，用途未知 |
| 7109 | `3202` | **`SetArmSdkStatus`**，同样只见于 H2 头文件 |
| 7110 / 7111 | `code=0` / `3202` | 用户开发模式，见上 |
| 7008–7010、7112 | `3203` | 未实现 |

由此反推出两个通用错误码的含义：**`3203` = 接口不存在，`3202` = 参数错误**（空参数导致）。
两者都不在 loco 的 73xx 表和 MotionSwitcher 的 70xx 表里。

⚠️ **扫描 api_id 时务必排除状态变更类接口。** 本次扫描误发了 `7110 SwitchToUserCtrl`（返回 0），
可能真的把机器人切进了用户开发模式而当时无人发布 `rt/user_lowcmd`；靠
`SwitchToInternalCtrl({"data":1})` 切回阻尼才恢复。**只读探测应限定在 7001–7006 这类 getter。**

便捷方法与实际调用的对应关系：

| 便捷方法 | 实际请求 | 官方描述与说明 |
|---|---|---|
| `ZeroTorque()` | `SetFsmId(0)` | 通过模式切换，进入零力矩模式。 |
| `Damp()` | `SetFsmId(1)` | 机器人进入阻尼模式。 |
| `Squat()` | `SetFsmId(2)` | 通过模式切换，进入下蹲模式。 |
| `Sit()` | `SetFsmId(3)` | 通过模式切换，进入落座模式。 |
| `StandUp()` | `SetFsmId(4)` | 通过模式切换，进入站立模式；模式 ID 表把 4 叫「锁定站立」（遥控器流程里的**预备状态**）。 |
| `Start()` | `SetFsmId(500)` | 官方原文：「通过模式切换，**进入主运控**」。❗ 它硬编码 500（常规运控），**在只支持走跑运控 801/802 的机器上会静默失败**。 |
| `StopMove()` | `SetVelocity(0, 0, 0)` | 将速度指令置零；默认有效 1 秒。 |
| `BalanceStand()` | `SetBalanceMode(0)` | 进入主运控的平衡站立模式；速度指令为 0 且机器人平稳时停止踏步，仅在必要时踏步。 |
| `ContinuousGait(flag)` | `SetBalanceMode(flag ? 1 : 0)` | 开启后机器人不断保持踏步，即使当前速度为 0。❗ 见下方官文矛盾说明。 |
| `HighStand()`/`LowStand()` | `SetStandHeight()` 的最大/最小无符号哨兵值 | 由固件解释为最高/最低站姿，不是实际米制高度。❗ 2026-07-29 实测：**这两条在走跑运控 (802) 下都没有任何效果**，见 FSM 实测表。 |
| `Move(vx, vy, vyaw)` | `SetVelocity(...)` | 速度指令默认生效 1 秒。 |
| `SwitchMoveMode(true)` | 只修改客户端本地布尔值 | 不发送 RPC；只让后续 `Move()` 把 duration 从 1 秒改为 864000 秒。 |
| `WaveHand()`/`ShakeHand()` | `SetTaskId(...)` | 触发仓库已知的内置任务；完整列表仍以固件查询结果为准。 |

官方对以上每个模式切换方法都附了同一句备注：**「最终状态切换是否成功取决于内置运控的状态切换逻辑」**。

❗ **`ContinuousGait()` 官文有两处矛盾描述**：一处说它修改 `Move()` 行为（那其实是 `SwitchMoveMode()`），
另一处说它是持续踏步。**以源码为准**：`ContinuousGait(flag)` = `SetBalanceMode(flag ? 1 : 0)`，
对应后一种。对手柄/导航控制建议关闭长时模式，按 5–10 Hz 刷新短 `duration`，输入超时后显式调 `StopMove()`。

### 高层运动模式

#### FSM ID

以下名称来自当前源码封装和与其对应的官方 G1 高层接口。不同固件可能增删模式，切换结果必须用 `GetFsmId()` 或匹配版本的 `/sportmodestate` 确认。

| FSM ID | 官方名称（模式 ID 说明） | 平衡控制 | 当前源码入口 |
|---:|---|---|---|
| 0 | 零力矩 | 无 | `ZeroTorque()` |
| 1 | 阻尼 | 无 | `Damp()` |
| 2 | 位控下蹲 | 无 | `Squat()` |
| 3 | 位控落座 | 无 | `Sit()` |
| 4 | 锁定站立 | 无 | `StandUp()` |
| 500 | 常规运控 | 有 | `Start()` 或 `SetFsmId(500)`。❗ **本机不可用**，见下方实测 |
| 501 | 常规运控 - 3-DoF waist | 有 | 仅 `SetFsmId(501)`。本机未单独验证 |
| 702 | 躺起 | 有 | 仅通用 `SetFsmId(702)`；无便捷方法。本机未验证 |
| 706 | 平衡下蹲、蹲起 | 有 | 仅通用 `SetFsmId(706)`。❗ **本机不可达**，见下方实测 |
| 801 | 走跑运控 | 有 | ✅ **唯一正确的命令值**。[官网：29-DoF 的 `ai_sport` 在 8.6.x.x 之后改为 802](https://support.unitree.com/home/zh/G1_developer/sport_services_interface)——改的是**回读编号** |
| 802 | 走跑运控（801 的回读编号） | 有 | ❌ **只是回读值，不能当命令发**！`SetFsmId(802)` 会进越障模式 812 |
| 812 | 越障模式（官方模式表外） | 有 | 实测由 **`SetFsmId(802)`** 进入；**不接受速度指令** |

**「常规运控」(500/501) 和「走跑运控」(801/802) 是两个并列的运控模式，不是前后步骤。**

#### ❗ 站高 / 平衡模式 / 摆动高度属于「常规运控」，本机没有

`SetStandHeight` 在 802 下完全没反应，不是用法不对，是**功能不在这套运控里**。三条证据：

1. **官方 G1 文档没有这些接口。** [高层运动服务接口](https://support.unitree.com/home/zh/G1_developer/sport_services_interface)
   的接口表里 `SetStandHeight` / `HighStand` / `LowStand` / `SetBalanceMode` / `SetSwingHeight`
   一个都没有——它们只存在于 SDK 头文件，是 Go2/B2 一脉的历史遗留。
2. **它们全由 LocoState 支撑。** 802 下 7003–7006 **整组**返回 7301，不是某一个坏了。
3. **本机只装了 `ai` 服务。** `CheckMode`(1001) = `{"form":"0","name":"ai"}`，
   `SelectMode("normal")` 返回 7004；常规运控 500/501 属于 `normal`，根本没安装。

⇒ **没有任何模式能同时提供「内置平衡 + 速度 + 站高」**：

| 模式 | 平衡 | 速度 | 站高 | |
|---|:---:|:---:|:---:|---|
| 4 锁定站立 | ❌ | ❌ | ? | 官方标注「无平衡控制」 |
| 802 走跑运控 | ✅ | ✅ | ❌ | 实测无效 |
| 500/501 常规运控 | ✅ | ✅ | ✅? | 服务未安装，不可达 |
| 706 平衡下蹲、蹲起 | ✅ | ? | ? | 实测不可达 |

706 实测（2026-07-29）：从 `fsm_id=4` 发 `SetFsmId(706)` 返回 `code=0`，但 25 s 内 `fsm_id`
一直停在 4，无任何副作用——与 500 表现一致。

⇒ **本机可用 FSM 只有 0 / 1 / 2 / 3 / 4 / 801(⇒802) / 812。**
`SetVelocity` 只有 `[vx, vy, omega]`，没有垂直分量，也不能靠它调高度。

⚠️ 不要盲扫 FSM ID：未知模式可能触发危险动作，而 `GetAvailableFsmIds`(7008) 本机未实现。

遥控器 `R2+A` 在本机进入 **802**。**不同机型的「主运控」编号本来就不同**——同一份 SDK 里
G1 的 `Start()` 是 `SetFsmId(500)`，H1 是 `SetFsmId(204)`。不要当通用常量硬编码。

##### 2026-07-29 实机实测（29-DoF，`mode_machine=5`）

| 发现 | 说明 |
|---|---|
| **命令值 ≠ 回读值，而且不能拿回读值当命令发** | `SetFsmId(801)` → `fsm_id` 0.07 s 瞬间经过 801，0.12 s 后稳定在 **802**。而 **`SetFsmId(802)` → 812（越障模式）**。官网那句「8.6.x.x 后更新为 802」改的**只是回读编号**。**命令只能发 801；判定成功用「回读 ∈ {801, 802}」，812 绝不能列入。** |
| **812 = 越障模式，且不吃速度指令** | 官方模式表里没有它，但《SportModeState 接口》承认「攀爬模式」存在。进入 812 后以 10 Hz 连发 `vx=0.2` 持续 5 s，`fsm_mode` 始终为 0；回到真正的 802，**同样的指令 0.23 s 就让 `fsm_mode` 翻到 1**。812 常自行回落到 802，所以会听到「越障 → 走跑」两次播报。早前猜测的「遥控器 R1+X/R1+Y 导致」已被推翻。 |
| **500 / 706 完全不可用** | 从 `fsm_id=4` 发 `SetFsmId(500)` 或 `SetFsmId(706)`，均返回 `code=0`，但 `fsm_id` 15–25 s 纹丝不动，也无任何副作用。根因是只装了 `ai` 服务，见上节。501/702 未单独验证。 |
| **速度指令在 802 下生效** | 尽管 7003–7006 全返回 7301，`SetVelocity`(7105) **是被接受的**：10 Hz 连发 `vx=0.2` 后约 **0.4 s** `fsm_mode` 从 0 翻到 1 并全程保持，停发后回 0。**不要因为 7301 就以为整个 loco 模块没了。** |
| **`SetStandHeight`(7104) 在 802 下无效** | 配对实测：`LowStand(0.0)` 与 `HighStand(UINT32_MAX)` 交替 4 轮，膝角（索引 3/9）两组只差 **0.0002 rad**，且每轮方向不一致。详见上节。 |
| **遥控器映射** | `L2+B` → 1 阻尼；`L2+UP` → 4 预备/锁定站立；`R2+A` → 802 走跑运控，**一步到位无中间态**。另有 `R1+X`（单自由度腰）/ `R1+Y`（三自由度腰），对应 500/501 一族。 |
| **`fsm_mode` 动态期真实存在** | 预备动作（站起来）期间 `fsm_mode` 持续为 **1** 约 5 s，完成后回 0。所以每步过渡都要等 `fsm_mode` 回 0 再发下一条。 |
| **零力矩不能从承重站姿直接进** | 从 `fsm_id=4`（承重）发 `SetFsmId(0)` 返回 `code=0` 但状态不变，机器人根本没卸力。**必须先切阻尼(1)，再从阻尼进 0。** 与官方「阻尼模式作为最终的保底模式，始终能够被切换」一致。 |

**固件拒绝不安全的跳转，且拒绝是静默的**：`SetFsmId` 返回 0 只代表请求被接受，
**任何切换都必须回读 `GetFsmId()` 校验**。官方对 `fsm_mode` 有两处措辞，含义相容：
`GetFsmMode()` 讲「0 站立 / 1 移动」，`SportModeState` 字段讲「0 静态、允许向外切换；
1 动态、不允许切换到大部分模式」。**阻尼模式作为最终的保底模式，始终能够被切换。**

##### 怎么判断一条指令是否真的生效

方法比结论更值得记住——前两种判据都误导过我们。

| 判据 | 可用性 |
|---|---|
| **响应码 `status.code`** | ❌ 所有 setter（7101/7102/7104/7105/7107）无论成败一律返回 0。实测机器人躺在地上零力矩、物理上什么都做不了时照样返回 0 |
| **关节角偏移** | ⚠️ 阈值靠拍脑袋，且人形起步慢—— 2 s 窗口里腿只动了 0.03 rad，据此误判过「速度指令无效」 |
| **`fsm_mode`(7002)** | ✅ 首选。固件自报的「0 站立 / 1 移动」 |

`fsm_mode` 实测特性：速度指令发出后 **0.2–0.5 s** 从 0 翻到 1，持续发送期间保持 1，停发立即回 0。
**机器人吊起来也能用**——它反映固件是否接受了指令，不依赖真的迈开腿。

⚠️ 轮询时**必须同时读 `fsm_id`(7001)**。曾经只盯 `fsm_mode`，没发现机器人早已跳到 812，
却还在按「802 下速度无效」解读数据。

**`fsm_mode` 不响应的参数（站高、摆动高度）只能观测关节角**，而 802 下机器人一直在做平衡调整，
单次测量分不开「响应」和「噪声」——单调扫描 `SetStandHeight` 时 `0.75` 那档测到 Δ膝角
`+0.26 rad`（约 15°），看着像强响应，实际是恰好迈了一步。可靠做法是**在两个极端值之间反复横跳**：

- 交替 ≥4 轮，每档等 4 s、只取末尾 1.5 s 的稳态均值（避开过渡）
- 判据是「**每轮方向都一致**」，不是「均值有差」——漂移做不到这一点
- **左右对称关节分开记**：两膝同向 = 蹲起，反向 = 迈步，用这个把平衡调整摘出去

#### Arm SDK 的可用模式

官方《高层运动服务接口》DDS 接口章节：

> DDS 接口支持上肢控制，**仅能在锁定站立、运控 1 与运控 2 中使用**。

✅ **2026-07-29 实测：`/arm_sdk` 在走跑运控 (802) 下可用**——权重置 1 后腰 + 双臂确实由本地命令接管，
所以「运控 1 与运控 2」至少涵盖走跑运控一族。可以确定它**在零力矩 (0)、阻尼 (1)、
位控下蹲 (2)、位控落座 (3) 下不可用**；上半身控制器激活前必须检查 `fsm_id`，不能盲发。
❗ **但接管期间腿部不再听 API 速度指令**，见下节。

帧内容——**除下列两项外，`LowCmd_.motor_cmd_` 的其余元素均无效**：

| 索引 | 含义 |
|---:|---|
| 12 – 28 | 腰部与上肢电机控制参数 |
| 29 | `MotorCmd_.q` 表示权重，取值范围 `[0.0, 1.0]` |

所以官方 example 不设 `crc`、`mode_machine`、`mode_pr`、`motor_cmd.mode` 不是疏忽——
这些字段在 `/arm_sdk` 路径上本来就不参与解析。

`g1_arm_action_client` 的错误码（`g1_arm_action_error.hpp`，注意**没有 7403**）：

| 错误码 | 含义 |
|---:|---|
| 7400 | `The topic rt/armsdk is occupied.` —— 有人在持续发 `/arm_sdk`，内置 Arm Action 让位 |
| 7401 | `The arm is holding. Expecting release action(99) or the same last action id.` |
| 7402 | `Invalid action id.` |
| 7404 | `Invalid fsm id.` —— 源码注释：动作只支持 fsm id `{500, 501, 801}`，且 801 下只支持 fsm_mode `{0, 3}` |

#### ❗ Arm SDK 接管与内置行走互斥（与官方说法冲突）

官方把 `/arm_sdk` 定位为「保留内置行走」「适合自定义上半身 + Unitree 下半身行走」。
**2026-07-29 实测：本机固件上二者无法共存。** 只要 `motor_cmd[29].q > 0`，`SetVelocity`(7105)
就完全不生效——响应码仍是 0，但机器人不进入移动状态。

消融实验（802，其余条件相同，只变 arm_sdk 一项）：不发 `/arm_sdk` ✅ 生效 ／ 发但 weight=0
✅ 生效 ／ 发且 weight=1 ❌ 无效。**所以冲突源自「权重接管」，不是「发布这个话题」本身。**

权重细扫（每档 4 s，原地转向 `omega=0.4`）：

| weight | 0.00 | 0.02 | 0.05 | 0.10 | 0.20 | 0.30 | 0.40 | 0.50 | 0.80 | 0.90 | 0.95 | 0.99 | 1.00 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 能走 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

**唯一能走的权重是 0，连 0.02 都掐断——是开关不是渐变。** 末尾重测 0.0 复现成功（排除状态漂移）。

⚠️ 这否掉了「上半身自定义 + 下半身内置行走并行」的架构，只能二选一或分时切换（走路时
weight=0，操作时停走再接管）。未验证的规避路径：`SetArmSdkStatus`(7109)。

#### Arm SDK 的权重进出语义

✅ 核实自 `unitree_sdk2/example/g1/high_level/g1_arm7_sdk_dds_example.cpp`。**进出不对称**：

| 阶段 | 权重 `motor_cmd[29].q` | 位置 12–28 |
|---|---|---|
| 进入 | **直接设 1.0，不做斜坡** | 从 `current_jpos` 用 2 s 插值到目标 |
| 退出 | 按 `weight_rate = 0.2 /s` 递减 | 保持不动 |

进入不需要权重斜坡，因为第一帧命令就等于实测位置，本来就没有跳变；退出必须斜坡，因为内置
程序此刻的上肢目标可能离你摆的姿势很远，直接切 0 会让手臂弹回去。

⚠️ **官方 example 的退出斜坡有缺陷，不要照抄**：`delta_weight = 0.2 × 0.02 = 0.004`，
而 `stop_time_steps = 2.0 / 0.02 = 100` 步，权重只从 `1.0` 降到 `0.6`，紧接着末帧直接写 `0`
—— **实际是个 0.6 → 0 的阶跃**。要走完斜坡，时长得取 `1.0 / weight_rate = 5 s`。
本工作区 `scripts/g1_upper_body_takeover.py` 已按 5 s 实现。

#### Balance Mode 与 Speed Mode

| 模式 | 值 | 含义 |
|---|---:|---|
| `balance_mode` | 0 | Balance Stand：零速稳定后不持续原地踏步。 |
| `balance_mode` | 1 | Continuous Gait：即使速度为零也保持踏步。 |
| `speed_mode` | 0 / 1 / 2 / 3 | 走跑运控下最大速度 1.0 / 2.0 / 2.7 / 3.0 m/s。 |

❗ `balance_mode` 属于常规运控，**本机不可用**；`speed_mode` 可用。

#### MotionSwitcher 模式

MotionSwitcher 管理“哪个运动服务占有机器人”，与 `fsm_id` 不是同一层状态机：

| API ID | 方法 | 作用 |
|---:|---|---|
| 1001 | `CheckMode(form, name)` | 查询当前运动服务；空 `name` 表示没有服务占用。**官方明确说明 `form` 无含义**，不要据此做判断。 |
| 1002 | `SelectMode(name_or_alias)` | 选择固件已安装的运动服务。 |
| 1003 | `ReleaseMode()` | 释放当前运动服务，供低层控制接管。 |
| 1004 | `SetSilent(bool)` | 设置 MotionSwitcher silent 状态。 |
| 1005 | `GetSilent(bool&)` | 查询 silent 状态。 |

MotionSwitcher 服务端错误码（与 Loco 的 73xx 是不同 endpoint 下的不同编号，别混用）：

| 错误码 | 官网中文 | SDK 原文（`motion_switcher_error.hpp`） |
|---:|---|---|
| 7001 | 请求参数错误。 | `parameter is invalid.` |
| 7002 | 切换服务繁忙，请稍后再试。 | `switcher is busy.` |
| **7003** | ❗ **官网表里漏了这一行** | `event is invalid.` |
| 7004 | 运控模式名不支持。 | `name or alias is invalid.` |
| 7005 | 运控模式内部指令执行错误。 | `name or alias is invalid.`（❗ SDK 里与 7004 文案重复，以官网中文为准） |
| 7006 | 运控模式检测指令执行错误。 | `check cmd execute error.` |
| 7007 | 运控模式切换指令执行错误。 | `select cmd execute error.` |
| 7008 | 运控模式释放指令执行错误。 | `release cmd execute error.` |
| 7009 | 自定义配置错误。 | `save customize data error.` |

注意 MotionSwitcher 的客户端在 SDK 里属于 `unitree::robot::b2` 命名空间（`b2/motion_switcher/`），
不在 `g1` 下——它是跨机型公用服务，所以错误码编号会和 Loco 的 73xx 撞号但含义无关。

#### ❗ 冷启动进程的第一个 MotionSwitcher 请求收不到回复

短命的命令行工具第一次跑必然超时，第二次就好。2026-08-19 定位，根因是 DDS 双向匹配不同步：

| 方向 | 冷启动后何时可用 |
|---|---|
| 我们 → switcher 的 request 订阅 | **2 ms**（`get_subscription_count()` 已是 1） |
| switcher → 我们的 response 订阅 | **要先真的发出一次请求才建立** |

所以第一个请求送达了、也被处理了，只是回复无处可去。`ros2 topic info` 这时全都显示正常。

实测冷启动每 200 ms 发一次 `CheckMode`：**#1 无回复，#2 起全部回复**，往返 9–18 ms。
**光等没用**——先 spin 最多 2 秒再发第一帧照样失败。连续快跑多个进程不复现（上一个留下的匹配还热着），静置十几秒后必复现。

⚠️ **重发只能用于查询类 API。** `ReleaseMode`(1003) 重发返回 **7002（switcher is busy）**，而第一条其实已经生效——工具报错退出，机器人却已经进了低层模式，比不重试更危险。正确做法是**收到任何一条回复后立即停止重发**，由开场那次 `CheckMode` 负责打通通道。长期运行的进程不受影响。

Loco 服务端错误码（来自 `g1_loco_client.hpp` 的 `UT_DECL_ERR`）：

| 错误码 | 含义 |
|---:|---|
| 7301 | LocoState not available：当前模式不提供该状态量。 |
| 7302 | Invalid fsm id。 |
| 7303 | Invalid task id。 |

G1 示例能识别 `normal`、`ai`、`advanced`，分别显示为 `sport_mode`、`ai_sport`、`advanced_sport`；
这只是名称映射，不保证每台机器人都装了全部服务（**本机只有 `ai`**）。
全身低层控制结束后应重新 `SelectMode()` 恢复此前服务，否则 `/api/sport/*` 不会重新可用。

**官方定义：`"ai"` 就是主运控。** 实测补充：`SelectMode("ai")` 之后运控服务的**起始 FSM 是 0（零力矩）**，
即开机状态——切入时有语音播报，之后 29 个关节 `MotorState.mode` 都回到 `1`（已通电），
但 `|tau_est|` 最大只有 0.26 N·m，关节可徒手推动。**这是「服务刚启动时停在 FSM 0」，
不是「`ai` 服务只能零力矩」**——同一个服务在 `fsm_id=4` 时踝关节实测有 9–15 N·m 的保持力矩。
「通电」和「出力」是两件事：零力矩下电机通电但不出力，`mode=1` 并不意味着关节在硬撑。

#### ❗ 待机的 `ai` 模式在以 1 kHz 发全零 `/lowcmd`

运控模式占着机器人时 `/lowcmd` 上不是空的。2026-08-19 实测（`ai` 停在 FSM 0，本机无任何 ROS 节点）：

| | `ai` 占用中 | `ReleaseMode`(1003) 之后 |
|---|---:|---:|
| `/lowcmd` 速率 | **1000 Hz** | **0 Hz** |
| 帧内容（400 帧 × 29 槽位全一致） | `mode=1`，`q`/`dq`/`tau`/`kp`/`kd` **全 0** | — |

即「使能但不出力」，与 FSM 0 的零力矩表现一致。两点后果：

- 零帧不把关节拉向任何目标，所以运控没释放时外部 `/lowcmd` **看起来能动**——两条流交替到达，固件执行最后收到的那一帧，命令被零帧稀释。症状是「动但发虚」而不是「一动不动」，很难归因。
- **`/lowcmd` 静默检查只能放在 `ReleaseMode` 之后**：释放前那 1 kHz 是运控自己的，查了必然误报；释放后总线是 0 Hz，一有流量才是真冲突。

### 低层模式和电机命令

| 字段/机制 | 含义与约束 |
|---|---|
| `LowCmd.mode_pr = 0` | PR 串联模式（默认）。**官方定义它同时作用于脚踝和腰部两处并联机构**，不只是脚踝。索引解释为 Pitch/Roll；本工作区的 URDF 和 ros2_control 默认要求该模式。 |
| `LowCmd.mode_pr = 1` | AB 并联模式，相同物理索引解释为 A/B 电机：`ANKLE_PITCH→ANKLE_B`、`ANKLE_ROLL→ANKLE_A`、`WAIST_ROLL→WAIST_A`、`WAIST_PITCH→WAIST_B`。不能把 A/B 角直接当作 URDF 的 Pitch/Roll 角。 |
| `mode_machine` | 机器人**型号**标识（不是步态模式）。低层示例从最新 `LowState.mode_machine` 读取并回填到 `LowCmd`。取值见下表。 |
| `MotorCmd.mode` | **官方定义：`0` = Disable，`1` = Enable。** |
| `q/dq/tau/kp/kd` | 位置、速度、前馈力矩、位置刚度和速度阻尼目标。低层控制器必须自行保证连续性、限位和稳定性。 |
| CRC | `/lowcmd` 必须按 HG `LowCmd` 布局计算 CRC；使用仓库已有 `motor_crc_hg` 实现，不要对序列化后的任意字节流自行猜测。 |
| 29/35 槽位 | 消息有 35 槽，G1 本体使用实际配置对应的前 29 槽；23-DoF 机型以及锁腰配置还会有不可用索引。 |
| `/arm_sdk` 权重 | `motor_cmd[29].q=0` 交还上肢，`=1` 完全接管；应在静止状态渐变权重，避免目标突跳。该特殊语义不适用于 `/lowcmd`。 |

#### `mode_machine` 取值：官网两页互相矛盾

两个官方页面给出了不一致的编号，必须以实机读数为准：

| 机型 | 《底层服务接口》的 IDL 注释 | 《关节电机顺序》页的标题 |
|---|---:|---:|
| 23-DoF | 4 | 1 |
| 29-DoF | **5** | 2 |
| 27-DoF（29-DoF 锁腰） | 6 | — |
| 14-DoF 双臂 | — | 9 |

✅ **本工作区实测：`mode_machine = 5`**，而设备确实是 29-DoF（腰 roll/pitch 与双腕 pitch/yaw 均存在）。
因此**《底层服务接口》的编号是对的，《关节电机顺序》页的 1/2/9 是旧编号**（该页更新日期 2025-03，早于底层页的 2025-10）。
不要用 `mode_machine == 2` 去判定 29-DoF。

#### 官方消息结构字段

来自《底层服务接口》的 IDL，补齐本仓 `.msg` 里没有注释的部分：

| 结构 | 字段 | 官方含义 |
|---|---|---|
| `IMUState_` | `quaternion[4]` | **顺序是 Qw Qx Qy Qz**（不是 ROS 惯用的 xyzw；`G1TopicSystem::decode_imu` 就是在做这个转换） |
| `IMUState_` | `gyroscope[3]` / `accelerometer[3]` / `rpy[3]` / `temperature` | 角速度 / 加速度 / 欧拉角 / IMU 温度 |
| `LowState_` | `tick` | 计时器，**每 1 ms 递增**。可用作固件侧时基，比 DDS 到达时刻可靠 |
| `LowState_` | `version[2]` | 版本 |
| `LowState_` | `wireless_remote[40]` | **宇树实体遥控器原始数据**。遥控器未开机/未连接时实测全 40 字节为 0；连上之后出现非零字节 |
| `MotorState_` | `ddq` | 关节反馈加速度 (rad/s²)，本仓未使用 |
| `MotorState_` | `q_raw` / `dq_raw` / `ddq_raw` | **官方标为「保留」**，不要当成未滤波的原始值使用 |
| `MotorState_` | `temperature[2]` | **外表与绕组温度** |
| `MotorState_` | `vol` | 电机端电压 |
| `MotorState_` | `sensor[2]` / `motorstate` | 传感器数据 / 电机状态（位域含义未公开） |

全身低层控制的一般顺序是：读取稳定的 `/lowstate` 和 `mode_machine`，通过 MotionSwitcher 释放内置运动服务，确认 `/lowcmd` 已经静默（**必须在释放之后查**，理由见上面「待机的 `ai` 模式在以 1 kHz 发全零 `/lowcmd`」），从当前反馈姿态建立首帧目标，再以固定周期发送带 CRC 的命令。退出时先回到稳定站姿并停止低层流，再恢复原 MotionSwitcher 服务。当前 `unitree_g1_ros2_control/G1TopicSystem` 已实现这套接管、静默等待、反馈时效检查和失败回滚，不应在旁边再运行官方低层示例。

### 退出低层控制必须主动卸力

**固件没有看门狗，停止发布 `/lowcmd` 不等于释放关节。** 最后一帧的 `kp`/`kd`/`q` 会被电机一直保持下去，手臂停在哪就顶在哪，持续吃电流直到过热。这是本工作区踩过的实际问题：`pkill` 掉控制栈后手臂仍在硬撑，肩部绕组升到 97 °C。

必须有人主动把增益降下来。降的方式也有讲究——**直接把 `q` 设成实测位置是错的**，那样 $k_p(q_{cmd}-q_{meas})$ 当场归零，手臂第一帧就失去支撑掉下去。正确做法是让命令保持在原来的偏移上，只缩放 $k_p$：

$$q_{cmd}=q_{meas}+\frac{\tau_{est}}{k_p},\qquad k_p^{cmd}=k_p\cdot s,\qquad s:1\to 0$$

$$\Rightarrow\ \tau=k_p^{cmd}\,(q_{cmd}-q_{meas})=\tau_{est}\cdot s$$

$k_p$ 被约掉，所以它取多少都不影响下降过程，只要为正。力矩从电机当前实际出力平滑淡出，交接处无阶跃。`kd` 全程保持原值、只在最后一帧归零——那时姿态已静止，撤掉阻尼不会引起运动。

需要注意静态下垂量是 $\tau_g/k_p$，$k_p\to 0$ 时按 $1/k_p$ 发散，所以**下降后半段必然比前半段快**，斜坡只能让它变缓、不能让它匀速。手臂最终一定停在自然下垂位：把它举着本身就要力矩，任何卸力都以下垂告终。

本工作区两条实现路径：

| 路径 | 位置 | 适用 |
|---|---|---|
| `G1TopicSystem::stop()` → `release_body()` | `unitree_g1_ros2_control` | 正常 `Ctrl-C` 关闭 `all_data.launch.py scope:=whole_body` 时自动执行，斜坡时长由参数 `release_ramp_s` 控制（默认 2 s，上限 30 s，设 0 关闭） |
| `ros2 run robot_bringup exit_debug_mode` | `robot_bringup` | 不走 ros2_control，直连 `/lowcmd` 与 MotionSwitcher。控制栈被强杀、崩溃或从未启动时的兜底 |

卸力之后还要 `SelectMode()` 交还内置运动服务，否则机器人停在无人占用的状态，`/api/sport/*` 不可用。

#### 关闭时发布必须用独立 context

这一点实测代价很大，单独记下：**rclcpp 在 context 关闭后会静默丢弃所有发布**，既不报错也不抛异常。而硬件插件的 `stop()` **只会在全局 context 已经开始关闭后才被调到**（`rclcpp::ok()` 在那里恒为 false），所以直接在 `stop()` 里发 `/lowcmd` 或 MotionSwitcher 请求，**一帧都不会上总线**。

实测对比（外部订阅者监听 `/lowcmd`）：

| 做法 | 关闭前最后一帧 | MotionSwitcher 请求 |
|---|---|---|
| 用全局 context 发布 | `kp=28.600`（斜坡完全丢失） | 0 条 |
| 用独立 context 发布 | `kp=0.000` | 1 条 `(1002, {"name":"ai"})` |

因此 `G1TopicSystem` 另建了一个 `shutdown_on_sigint = false` 的私有 context，专用于卸力斜坡和模式交还。同理，关闭路径上不能用任何需要等响应的服务调用——executor 已停，回复永远不会到达，只会阻塞到超时。

#### 停控制栈只能用 SIGINT

上面这套清理**全部挂在 SIGINT 上**。`pkill` 默认发 SIGTERM，rclpy 根本不处理，rclcpp 也来不及跑完关闭序列。本仓库因此踩过三次坑：

| 被跳过的清理 | 后果 |
|---|---|
| `G1TopicSystem::stop()` 的卸力斜坡 | 关节保持最后一帧命令持续吃电流，肩部绕组升到 97 °C |
| `gloria_ros` 的 `disable_on_shutdown` | 两只 Gloria-M 一直留在使能态 |
| `native_bridge_node` 的 USB 传输取消 | CANalyst-II 固件卡死，之后每次启动都报 `LIBUSB_ERROR_TIMEOUT`，**只能物理拔插复位** |

而且信号**只能发给 launch 进程**，由它按依赖顺序转发给子进程：

```bash
kill -INT "$(pgrep -f 'ros2 launch robot_bringup all_data' | head -1)"
```

发给整个进程组（`kill -INT -- -PID`）会让每个子进程被直接杀死，launch 来不及编排关闭，效果等同 `pkill`。另外在脚本里用 `(cmd &)` 后台启动 launch 会让它继承 `SIG_IGN`，此后 SIGINT 完全无效——调试关闭逻辑时这两点都会让人误判成代码有问题。

不要把需要物理复位的 `LIBUSB_ERROR_TIMEOUT` 和 libusb completion 的 `status 6` 混淆。`status 6 = LIBUSB_TRANSFER_OVERFLOW`，表示单次 RX 缓冲装不下设备返回的数据，不是设备断开；native bridge 会丢弃该 transfer、计数并有界重提交。只要 `lsusb -d 04d8:0053` 仍能看到设备且重启后正常起流，就不需要插拔。

### MotorState.mode 与故障字段

`LowState.motor_state[i]` 里有三个字段用于判断电机状态。任何“手臂突然变紧、不听指令”的现象都应先查这三项，再去查控制链路。

| 字段 | 含义 |
|---|---|
| `mode` | `1` = 已通电，`0` = 未通电。**这只反映有没有人在给它发使能命令，不是故障标志。** 停掉 `/lowcmd` 发布者后全部关节会读到 `0`；交还 `ai` 模式后内置服务会把它们重新置 `1`——机制就是那 1 kHz 全零帧里的 `MotorCmd.mode = 1`。 |
| `temperature[0]` / `temperature[1]` | 两路测温，实测 `[0]` 接近外壳、`[1]` 接近绕组。两者差值大说明是持续大电流发热而非环境温度高。 |
| `motorstate` | 位域故障码，非 0 即异常。实测过热的 `right_shoulder_pitch` 上出现过 `512`；该编码未经官方文档确认。 |

2026-07-27 实测的一次异常：`right_shoulder_pitch` 在 75 °C 时 `motorstate` 变为 `512`，同时右臂 7 个电机全部读到 `mode=0`，而其中最凉的 `right_wrist_yaw` 只有 39 °C。当时左臂 `left_shoulder_pitch` 绕组已达 97 °C 却仍是 `mode=1`——**更热的那个反而没有降级，所以不能断定 `512` 就是过热保护**。整条手臂一起变 `mode=0` 的机制也未经确认。记录现象备查，不要据此推断因果。

电机降级或失去命令后不再跟随位置指令，`tau_est` 掉到约 0，只剩绕组阻尼，**手感是“紧”而不是“软”**，很容易误判成软件把命令锁死了。

### 肩关节热负荷

肩 pitch 是最容易过热的关节。G1 的关节电机没有减速自锁，保持姿态完全靠持续电流，而肩 pitch 要独自扛住整条手臂。手臂水平伸出时该关节约 20 N·m，是最恶劣的持续工况。2026-07-27 实测：左臂在水平位悬停数分钟后 `left_shoulder_pitch` 绕组到 **97 °C**（外壳 73 °C）；把手臂放回自然下垂位后重力力矩接近 0，温度即回落，十几分钟内降到 53 °C。

由此带来的使用约束：

- 重力补偿做得越准，电机越会老实地长期顶住任意姿态，**热负荷比标定时高一个量级**。标定是一个个静态点各停几秒，长时间悬停是完全不同的工况。
- 任何让手臂长时间停在水平位附近的功能（零重力示教、保持姿态 demo）都要限制时长，用完及时退出调试模式。

### Arm Action、Arm SDK 与示例入口

Arm Action 的 `/api/arm/*` 支持：

| API ID | 方法 | 含义 |
|---:|---|---|
| 7106 | `ExecuteAction(action_id)` | 执行固件预定义动作。 |
| 7107 | `GetActionList(string&)` | 获取当前固件支持的动作列表。 |

若 `/arm_sdk` 正在发布，Arm Action 返回 7400；FSM 不满足条件时返回 7404。旧版错误字符串可能把 `rt/arm_sdk` 写成 `rt/armsdk`，实际 ROS 2 示例话题仍是 `/arm_sdk`。`g1_arm_sdk_dds_example` 通过编译期 `ARM_TYPE` 在 5-DoF 臂和 7-DoF 臂映射间选择，必须与实机硬件一致。

`unitree_ros2_example` 中 G1 专用可执行文件如下：

| 可执行文件 | 控制路径 | 用途/注意事项 |
|---|---|---|
| `g1_loco_client_example` | `/api/sport/*` | 查询和设置全部 Loco API。 |
| `g1_arm_action_example` | `/api/arm/*` | 查询并执行预定义上肢动作。 |
| `g1_arm_sdk_dds_example` | `/arm_sdk` + `/lowstate` | 在保留内置行走时接管腰部和双臂。 |
| `g1_low_level_example` | `/lowcmd` + `/lowstate` | 500 Hz 全身低层示例；会控制本体，不可与内置行走并行。 |
| `g1_ankle_swing_example` | `/lowcmd` + `/lowstate` + `/secondary_imu` | PR/AB 脚踝低层示例。 |
| `g1_dual_arm_example` | MotionSwitcher + `/lowcmd` | 条件编译；先 `ReleaseMode()` 再进入低层控制，不是可与行走并行的 Arm SDK 示例。 |
| `g1_dex3_example` | `/dex3/*` | 左右 Dex3 命令和状态。 |
| `g1_audio_client_example` | `/api/voice/*` | TTS、PCM、音量和 RGB 灯。 |

源码入口见 [`src/unitree_ros2/example/src/include/g1`](src/unitree_ros2/example/src/include/g1)、[`src/unitree_ros2/example/src/src/g1`](src/unitree_ros2/example/src/src/g1) 和官方 [G1 High-level motion service interface](https://support.unitree.com/home/en/G1_developer/sport_services_interface)。
